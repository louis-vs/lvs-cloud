apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrepositories
  - cert-manager
  - postgresql-new
  # - flux-image-automation  # Requires flux image-automation-controller (not installed by default)
