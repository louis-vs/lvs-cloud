apiVersion: v1
kind: Service
metadata:
  name: {{ include "tiny-screen-api.fullname" . }}
  labels:
    {{- include "tiny-screen-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tiny-screen-api.selectorLabels" . | nindent 4 }}
