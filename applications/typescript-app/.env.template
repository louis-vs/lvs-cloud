# TypeScript App Environment Configuration
# This template is processed by envsubst during deployment
# GitHub Actions will inject actual secret values

# Database Configuration
DATABASE_URL=postgresql://ts_app_user:${POSTGRES_TS_PASSWORD}@postgresql:5432/ts_app

# Application Environment
NODE_ENV=production
