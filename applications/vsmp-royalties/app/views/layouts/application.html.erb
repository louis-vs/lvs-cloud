<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Vsmp Royalties" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Vsmp Royalties">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Simple.css - classless CSS framework %>
    <link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <h1>VSMP Royalties</h1>
      <nav>
        <%= link_to "Imports", imports_path %> |
        <%= link_to "Statements", statements_path %> |
        <%= link_to "Writers", writers_path %> |
        <%= link_to "Works", works_path %>
      </nav>
    </header>

    <main>
      <% if notice.present? %>
        <p style="color: green"><%= notice %></p>
      <% end %>
      <% if alert.present? %>
        <p style="color: red"><%= alert %></p>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
