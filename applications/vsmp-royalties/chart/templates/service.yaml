apiVersion: v1
kind: Service
metadata:
  name: {{ include "vsmp-royalties.fullname" . }}
  labels:
    {{- include "vsmp-royalties.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vsmp-royalties.selectorLabels" . | nindent 4 }}
